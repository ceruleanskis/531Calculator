/**
 * Constant strings for the calculator.
 */

export enum Bar {
    STANDARD = 'standard',
    CURL = 'curl'
}

export enum CalcMethod {
    TMAX = 'tmax',
    ONERM = '1rm',
    WARMUP = 'warmup',
    PERCENTAGE = 'percent',
}

export enum Lift {
    BENCH_PRESS = 'bp',
    SQUAT = 'squat',
    OVERHEAD_PRESS = 'ohp',
    DEADLIFT = 'dl'
}